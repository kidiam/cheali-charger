
set(execName "${PROJECT_NAME}-${hardware}-${cheali-charger-buildnumber}-${cheali-charger-version}")
set(sizeName "${PROJECT_NAME}-${hardware}")

include_directories(${CMAKE_SOURCE_DIR}/src/core/ARM-Cortex-M0)
include_directories(${CMAKE_SOURCE_DIR}/src/core)
include_directories(${CMAKE_CURRENT_SOURCE_DIR})

link_libraries(m)

add_executable(${execName} ${ALL_SOURCE_FILES})

